interface:
  display_name: "Adk Utils Example"
  short_description: "Repo-specific patterns and workflows for adk-utils-example"
  default_prompt: "Use the adk-utils-example repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true